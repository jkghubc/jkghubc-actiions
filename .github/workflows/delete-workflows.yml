name: Delete Workflows

on: [workflow_dispatch]

env:
  adbneme: ${vars.DATABRICKS_NAME}

jobs:
  do:
    runs-on: ubuntu-latest
    steps:
      - name: run simple shell
        run: |
         echo "hello "
         echo " $adbname"
         
